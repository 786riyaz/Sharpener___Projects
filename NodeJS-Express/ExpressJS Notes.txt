Express is a Library
npm i express
npm i express --save
=====================================================================
NodeJS Server Creation ::
const http = require("http");
const server = http.createServer((req,res)=>{
    Code..
})
server.listem(3000);

ExpressJS Server Creation ::
const http = require("http");
const express = require ("express");
const app = express();
const server = http.createServer(app);
server.listen(3000)
=====================================================================
const http = require("http");
const server = http.createServer(app);

server.listen(3000, () => {
    console.log("Server is running on port 3000");
});
||
||
||
||
\/
const express = require("express");
const app = express();

app.listen(3000, () => {
    console.log("Server is running on port 3000");
});
=====================================================================
Middleware ::
Checkpoints for requests untill the final response

=====================================================================
=====================================================================
=====================================================================

